apiVersion: velero.io/v1
kind: Schedule
metadata:
  name: backup-staging
  namespace: velero
spec:
  schedule: "0 3 * * 0"
  skipImmediately: false
  template:
    includedNamespaces:
      - staging
    ttl: 720h0m0s
    metadata: {}
    hooks: {}
